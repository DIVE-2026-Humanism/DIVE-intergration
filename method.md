# method.md — 부산 청년 1인가구 경제 계층 6유형 분류 파이프라인

> DIVE 2026 GLOBAL DATA HACKATHON · 부산광역시 × KCB 공동 발제
> 이 문서는 Claude Code가 그대로 구현할 수 있도록 작성된 구현 명세서입니다.
> 범위: **분석 파이프라인만** (웹 UI·정책 매칭 엔진은 이 문서 범위 밖)

---

## 0. 목적

KCB 신용 마이크로데이터 10만 행을 입력받아, 부산 청년 1인가구를 **6개 경제 계층 유형**으로 분류하고, 유형별 프로파일과 검증 리포트, 그리고 **8문항 축소 분류기**를 산출한다.

핵심 설계 원칙 3가지:

1. **KCB 데이터는 서비스 입력이 아니라 라벨 생성기다.** 실제 사용자는 42개 금융변수를 제공할 수 없다. 배치로 정밀 라벨을 만들고, 사용자가 답할 수 있는 8문항으로 그 라벨을 재현하는 축소 모델을 학습한다.
2. **최종 라벨은 규칙 기반이다.** 행정에서 재현·감사 가능해야 하므로 군집 결과를 그대로 쓰지 않는다. 군집은 규칙의 정당성을 검증하는 근거로만 쓴다.
3. **모든 임계값은 분위수 기준이다.** 합성데이터의 스케일이 실제와 다를 수 있으므로 절대값 임계값(예: DSR 40%)을 쓰지 않는다.

---

## 1. 실행 환경

```
Python >= 3.10
pandas, numpy, scikit-learn, scipy, matplotlib, seaborn
lightgbm  (설치 실패 시 sklearn.ensemble.HistGradientBoostingClassifier로 폴백)
joblib
```

`requirements.txt`를 생성할 것.

---

## 2. 입력 데이터 명세

### 2.1 파일

- CSV 1개, **100,816행 × 42열** (헤더 1행 포함 100,817줄)
- 인코딩: `utf-8-sig` 우선 시도, 실패 시 `cp949` 폴백
- 경로는 CLI 인자 `--input`으로 받는다. 하드코딩 금지.

### 2.2 컬럼 (원본 헤더 그대로)

```
성별
연령대
직업군
거주지 시군구 코드
근무지 시군구 코드
추정월소득
증빙연소득
추정 연소득
2년전 추정 연소득 금액
총자산평가금액(주택)
순자산평가금액(주택)
자가거주여부
현 거주지의 아파트여부
현 거주지의 매매가(국토부 실거래가) 또는 공시가격
차량보유(국산/수입)
추정 LTV
추정DTI
신용평점
총대출건수
신용대출-총대출약정액
신용대출-총대출잔액
주택담보대출-총대출약정액
주택담보대출-총대출잔액
정책자금대출-총대출약정액
정책자금대출-총대출잔액
총 대출 상환금액 (최근 12개월)
최근 12개월 신용카드소비금액
최근 12개월 체크카드소비금액
최근 12개월 일시불이용금액
최근 12개월 할부이용금액
최근 12개월 현금서비스이용금액
대출연체건수
카드연체건수
연체일수
대출연체금액
카드연체금액
Thin Filer 여부
파산, 개인회생 신청 여부
2년내 현거주지평균실거래가
2년내 현거주지평균전세거래가
2년내 직장명이력건수
2년내 이직후 소득 증감액
```

> `파산, 개인회생 신청 여부`는 원본에서 큰따옴표로 감싸져 있다. pandas 기본 파서가 처리하지만, 컬럼명 접근 시 쉼표 포함 문자열임에 유의.

### 2.3 사전 확인된 데이터 특성 (재조사 불필요)

구현 시 이미 확인된 사실이므로 그대로 전제해도 된다. 단 assert로 검증은 넣을 것.

| 항목 | 실측값 |
|---|---|
| 총 행 수 | 100,816 |
| 결측 센티널 | `-99999999` (주로 실거래가·전세거래가 컬럼) |
| 근무지 시군구 코드 결측·0 | **0건** — 전원 근무지 보유 |
| 추정월소득 = 0 | **0건** |
| 추정 연소득 = 0 | **0건** |
| 증빙연소득 = 0 | **68,023건 (67.5%)** |
| 신용평점 = 150 (하한 절단) | **2,672건** |

**직업군 코드 분포 (7종, 결측 없음)**

| 코드 | 건수 | 비중 |
|---|---:|---:|
| 420 | 62,725 | 62.2% |
| 910 | 17,625 | 17.5% |
| 510 | 15,919 | 15.8% |
| 410 | 2,080 | 2.1% |
| 520 | 1,159 | 1.1% |
| 440 | 903 | 0.9% |
| 430 | 405 | 0.4% |

### 2.4 구현 전 반드시 확인해야 할 미해결 항목

파이프라인 시작 시 아래를 점검하고 `outputs/data_caveats.md`에 기록할 것.

1. **`추정가구원수` 컬럼이 없다.** 발제자료는 "추정가구원수 = 1 필터링으로 1인가구 추출"을 명시하지만 제공 CSV에는 해당 컬럼이 없다. → 데이터가 이미 1인가구로 필터링되어 배포된 것으로 **가정**하고, 이 가정을 리포트 상단에 명시. 발제사 확인 필요 항목으로 표기.
2. **KCB 직업군 코드북 미확보.** 410/420/430/440/510/520/910의 의미를 모른다. → 원핫 인코딩으로만 사용하고 **의미를 부여하지 말 것**. 코드북 확보 시 재해석.
3. **금액 단위 미확정.** 추정월소득 2750, 추정 연소득 32086 형태로 보아 **천원 단위**로 추정된다. `추정월소득 × 12`와 `추정 연소득`의 비율 분포를 계산해 리포트에 기록하고, 단위 가정을 명시할 것.
4. **상수·준상수 컬럼.** `자가거주여부`, `추정 LTV`, 주택담보대출 관련 컬럼은 샘플상 거의 단일값이다. 고유값 1개이거나 최빈값 비중 99% 초과인 컬럼을 자동 탐지해 리포트에 나열하되, **자동 삭제하지 말고 로그만 남길 것.**

---

## 3. 모듈 구조

```
project/
├── requirements.txt
├── src/
│   ├── __init__.py
│   ├── config.py          # 컬럼명 상수, 센티널, 임계값 파라미터
│   ├── load.py            # 로딩 + 전처리 + 데이터 점검
│   ├── features.py        # 파생변수 생성
│   ├── scores.py          # 축 스코어 산출 (분위수 정규화 + PCA 가중)
│   ├── segment.py         # 6유형 분류 (규칙) + GMM 교차검증
│   ├── validate.py        # 검증 리포트 + 시각화
│   ├── reduced_model.py   # 8문항 축소 분류기 학습
│   └── run.py             # 오케스트레이션 (CLI 진입점)
└── outputs/
    ├── segments.csv
    ├── segment_profile.csv
    ├── classifier.pkl
    ├── decision_rules.txt
    ├── validation_report.md
    ├── data_caveats.md
    └── figures/
```

실행:

```bash
python -m src.run --input data/kcb.csv --outdir outputs/ [--seed 42]
```

모든 난수는 `--seed`로 고정. 재현성 필수.

---

## 4. STEP 1 — 로딩 및 전처리 (`load.py`)

1. CSV 로딩 (인코딩 폴백 처리)
2. `assert len(df) == 100816` — 불일치 시 경고 출력 후 진행
3. **센티널 처리**: 전 수치 컬럼에서 `-99999999` → `np.nan`. 동시에 원 컬럼별 `{col}__missing` 불리언 플래그 컬럼 생성.
   - **중요**: 결측 자체가 신호다(비아파트·다세대 등 취약주거 프록시 가능성). 결측 행을 삭제하지 말 것.
4. 상수·준상수 컬럼 탐지 → `data_caveats.md`에 기록
5. 단위 검증: `추정월소득 * 12 / 추정 연소득` 분포 요약 기록
6. 처리된 DataFrame 반환

---

## 5. STEP 2 — 파생변수 (`features.py`)

전 산식에 대해 **분모가 0 또는 NaN이면 결과는 NaN**으로 처리한다. 0으로 채우지 말 것 (의미 왜곡).
각 파생변수마다 NaN 비율을 리포트에 기록한다.

### 5.1 소득·고용

| 변수명 | 산식 |
|---|---|
| `income_verify_ratio` | `증빙연소득 / 추정 연소득` |
| `has_verified_income` | `증빙연소득 > 0` |
| `income_trajectory` | `(추정 연소득 - 2년전 추정 연소득 금액) / 2년전 추정 연소득 금액` |
| `income_declined` | `income_trajectory < 0` |
| `irregular_income_ratio` | `(추정 연소득 - 추정월소득 * 12) / 추정 연소득` |
| `job_turnover` | `2년내 직장명이력건수` (그대로) |
| `job_change_return` | `2년내 이직후 소득 증감액 / 추정월소득` |
| `downward_move` | `(job_turnover >= 1) & (2년내 이직후 소득 증감액 < 0)` |

### 5.2 주거

| 변수명 | 산식 |
|---|---|
| `jeonse_income_multiple` | `2년내 현거주지평균전세거래가 / 추정 연소득` |
| `pir` | `2년내 현거주지평균실거래가 / 추정 연소득` |
| `housing_equity_ratio` | `순자산평가금액(주택) / 총자산평가금액(주택)` |
| `commute_mismatch` | `거주지 시군구 코드 != 근무지 시군구 코드` |
| `housing_price_missing` | 실거래가 결측 플래그 (STEP 1 산출물 재사용) |

### 5.3 부채·상환

| 변수명 | 산식 |
|---|---|
| `dsr` | `총 대출 상환금액 (최근 12개월) / 추정 연소득` |
| `credit_limit_util` | `신용대출-총대출잔액 / 신용대출-총대출약정액` |
| `total_loan_balance` | 신용 + 주담대 + 정책자금 **잔액** 합 |
| `avg_loan_balance` | `total_loan_balance / 총대출건수` |
| `multi_debt` | `총대출건수 >= 3` |
| `policy_loan_user` | `정책자금대출-총대출잔액 > 0` |

### 5.4 소비·유동성

| 변수명 | 산식 |
|---|---|
| `consumption_ratio` | `(신용카드소비 + 체크카드소비) / 추정 연소득` |
| `credit_dependency` | `신용카드소비 / (신용카드소비 + 체크카드소비)` |
| `installment_ratio` | `할부이용액 / (일시불이용액 + 할부이용액)` |
| `cash_advance_flag` | `현금서비스이용금액 > 0` |

### 5.5 신용·연체

| 변수명 | 산식 |
|---|---|
| `total_delinq_cnt` | `대출연체건수 + 카드연체건수` |
| `delinq_severity` | `(대출연체금액 + 카드연체금액) / 추정월소득` |
| `delinq_days_band` | `연체일수` 구간화: `0 / 1-30 / 31-90 / 91+` |
| `score_floor` | `신용평점 == 150` |
| `thin_filer` | `Thin Filer 여부` (그대로) |

산출물: `outputs/features_summary.csv` — 파생변수별 건수, 평균, 중앙값, 표준편차, NaN 비율, 분위수(10/25/50/75/90).

---

## 6. STEP 3 — 축 스코어 (`scores.py`)

### 6.1 축 구성

**X축 `financial_stress_score` (재무 스트레스, 높을수록 나쁨)**

```
dsr                (+)
credit_limit_util  (+)
delinq_severity    (+)
cash_advance_flag  (+)
installment_ratio  (+)
신용평점            (-)   ← 방향 반전
```

**Y축 `employment_instability_score` (고용 불안정, 높을수록 나쁨)**

```
job_turnover           (+)
downward_move          (+)
income_trajectory      (-)   ← 방향 반전
irregular_income_ratio (+)
has_verified_income    (-)   ← 방향 반전
```

### 6.2 산출 절차

1. **방향 통일**: `(-)` 표시 변수는 부호 반전. 모든 구성변수가 "높을수록 나쁨"이 되도록 한다.
2. **분위수 정규화**: 각 구성변수를 `rank(pct=True) * 100`으로 0~100 변환.
   - NaN은 순위 계산에서 제외한 뒤 **중앙값(50)으로 대체**하고, `{var}__imputed` 플래그를 남긴다.
3. **가중치 산출**: 축별 구성변수 행렬에 PCA를 적용, **제1주성분 로딩의 절대값을 합이 1이 되도록 정규화**해 가중치로 사용한다.
   - 제1주성분 설명분산비율을 리포트에 기록.
   - 설명분산비율이 0.4 미만이면 "축 일관성 낮음" 경고를 출력하고, 균등가중 결과도 함께 산출해 비교한다.
4. **최종 스코어** = 가중합, 0~100 범위.

> 균등가중이 아니라 PCA 가중을 쓰는 이유: 발표에서 "가중치를 데이터가 정했다"고 방어할 수 있다. 다만 균등가중 결과와의 상관계수를 함께 제시해 강건성을 보인다.

---

## 7. STEP 4 — 6유형 분류 (`segment.py`)

### 7.1 분류 순서 (순서 엄수)

**(1) 특수 유형 사전 분리** — 결정적 규칙, 모델 불필요

```
T6  회생·파산 진행군
    조건: `파산, 개인회생 신청 여부` == 1

T5  신용 무이력군 (Thin Filer)
    조건: (Thin Filer 여부 == 1) AND (총대출건수 == 0) AND (score_floor == True)
```

- T6를 **먼저** 분리한 뒤 잔여에서 T5를 분리한다 (중복 배정 방지).
- T5 분리 사유: 신용평점 150은 "취약해서"가 아니라 "정보가 없어서" 낮다. 분리하지 않으면 T3·T4를 오염시킨다.
- **T5 조건이 지나치게 좁아 100건 미만이 나오면**, `총대출건수 == 0` 조건을 완화한 대안 정의도 함께 산출해 리포트에 병기할 것.

**(2) 주 분류** — 잔여 행에 2×2 적용

각 축 스코어의 **중앙값**을 임계값으로 한다. `config.py`에서 분위수 파라미터로 조정 가능하게 구현 (기본 0.5).

| 유형 | 조건 | 명칭 |
|---|---|---|
| `T1` | 재무 안정 & 고용 안정 | 자산형성 준비군 |
| `T2` | 재무 취약 & 고용 안정 | 고정지출 압박군 |
| `T3` | 재무 안정 & 고용 불안 | 잠재 불안군 |
| `T4` | 재무 취약 & 고용 불안 | 복합 위기군 |

### 7.2 주거 부담 수정자 (H flag)

각 유형에 독립적으로 부착한다.

```
H = (jeonse_income_multiple 상위 25%) OR (commute_mismatch == True)
```

표기: `T2-H`, `T3-H` 등. 운영상 12구분, 발표는 6유형 기준.

### 7.3 크기 점검

- 각 유형 비중을 계산. **T6를 제외하고 5% 미만인 유형이 있으면 경고 출력.**
- **T3 규모를 리포트 최상단에 별도 강조 출력할 것.** T3가 5% 미만이면 다음 경고를 명시적으로 출력:

  ```
  [WARNING] T3(잠재 불안군) 비중이 임계 이하입니다.
  합성데이터가 고용 불안정과 재무 악화를 강하게 연동시켜 생성되었을 가능성이 있습니다.
  '사각지대' 서사 대신 '고용 불안정 → 재무 악화 직결' 서사로 전환을 검토하십시오.
  ```

  이 경고는 프로젝트 전략을 가르는 지점이므로 반드시 구현할 것.

### 7.4 GMM 교차검증

최종 라벨을 대체하지 않는다. 규칙의 정당성 근거로만 산출한다.

1. 입력: 두 축 스코어 + 주요 파생변수(표준화)
2. `k = 2..8` 범위에서 GaussianMixture 적합, **BIC**와 **실루엣 계수** 곡선 산출
3. 최적 k의 군집 라벨과 T1~T4 규칙 라벨의 **교차표**, **Adjusted Rand Index**, **최적 매칭 기준 일치율(%)** 산출
4. 리포트에 "데이터가 자연 형성한 군집과 규칙 분류가 XX% 일치" 문장으로 기록

---

## 8. STEP 5 — 8문항 축소 분류기 (`reduced_model.py`)

### 8.1 목적

42개 금융변수로 만든 6유형 라벨을, **사용자가 실제로 답할 수 있는 8개 입력만으로** 재현하는 모델. 서비스화의 핵심.

### 8.2 입력 피처 8종 (원본 컬럼에서 구성)

| # | 피처 | 원본 매핑 | 사용자 문진 대응 |
|---|---|---|---|
| 1 | `age_band` | 연령대 | 나이 |
| 2 | `gender` | 성별 | 성별 |
| 3 | `region` | 거주지 시군구 코드 | 사는 구·군 |
| 4 | `income_band` | 추정월소득 → 5분위 구간화 | 월 실수령액 |
| 5 | `housing_cost_band` | 2년내 현거주지평균전세거래가 → 구간화 (결측은 별도 범주) | 전세보증금 / 월세 |
| 6 | `debt_band` | 총대출건수 + 신용대출 잔액 → 구간화 | 대출 유무·대략 잔액 |
| 7 | `job_turnover` | 2년내 직장명이력건수 | 최근 2년 이직 횟수 |
| 8 | `distress_flag` | `cash_advance_flag OR (total_delinq_cnt > 0)` | 최근 연체·현금서비스 경험 |

**구간화는 반드시 분위수 기준**으로 하고, 경계값을 `outputs/binning.json`에 저장해 서비스단에서 재사용 가능하게 할 것.

### 8.3 학습

- 타깃: 6유형 라벨
- 모델: LightGBM (폴백: `HistGradientBoostingClassifier`)
- 검증: **Stratified 5-Fold**
- 지표: macro F1, accuracy, 클래스별 precision/recall, confusion matrix
- 클래스 불균형(T6 등)에 `class_weight='balanced'` 적용
- `predict_proba` 최대값(확신도) 분포를 산출 → 확신도 낮은 구간 비율 리포트 (서비스에서 추가 질문 트리거 근거)

### 8.4 해석용 모델 병행

`DecisionTreeClassifier(max_depth=4)`를 같은 피처로 학습해 `export_text`로 규칙을 `outputs/decision_rules.txt`에 저장. 발표 시 판정 근거 제시용.

### 8.5 산출

- `classifier.pkl` (joblib, 전처리 파이프라인 포함)
- 리포트에 **"42개 금융변수 기반 라벨을 8문항으로 XX% 재현"** 문장 기록. 이 수치가 발표 핵심 지표다.

---

## 9. STEP 6 — 검증 리포트 (`validate.py`)

`outputs/validation_report.md`를 생성한다. 포함 항목:

1. **데이터 개요** — 행 수, 결측 현황, 상수 컬럼, 단위 가정
2. **유형별 규모** — 건수·비중 표, T3 강조
3. **세그먼트 프로파일** — 유형 × 주요 지표(추정월소득, dsr, credit_limit_util, 신용평점, job_turnover, income_trajectory, jeonse_income_multiple, 연체율)의 평균·중앙값 표. `segment_profile.csv`로도 저장.
4. **유형 간 차이 검정** — 주요 지표별 일원배치 ANOVA + **eta squared(효과크기)**. p값만 쓰지 말 것 (n=10만이라 거의 다 유의하게 나옴). 효과크기 중심으로 해석.
5. **교차표** — 유형 × 연령대, 유형 × 성별, 유형 × 거주 구군, 유형 × 직업군
6. **GMM 교차검증 결과** — BIC/실루엣 곡선, 일치율, ARI
7. **축소 분류기 성능** — confusion matrix, macro F1, 확신도 분포
8. **상관행렬** — 원본 주요 변수 + 파생변수. **파생변수가 원본보다 유형 분리에 기여하는지 반드시 확인해 기록.**

### 시각화 (`outputs/figures/`)

- `segment_scatter.png` — 2축 산점도, 유형별 색상, 사분면 경계선
- `segment_size.png` — 유형별 규모 막대
- `correlation_heatmap.png`
- `gmm_selection.png` — BIC/실루엣 곡선
- `confusion_matrix.png`
- `radar_by_segment.png` — 유형별 4축(소득안정/주거압박/상환여력/유동성) 레이더

---

## 10. 산출물 명세

| 파일 | 내용 |
|---|---|
| `segments.csv` | `row_id, segment, segment_name, H_flag, financial_stress_score, employment_instability_score` + 주요 파생변수 |
| `segment_profile.csv` | 유형별 통계 요약 (발표 표로 직행) |
| `features_summary.csv` | 파생변수별 기술통계·NaN 비율 |
| `classifier.pkl` | 8문항 축소 분류기 (전처리 포함) |
| `binning.json` | 구간화 경계값 |
| `decision_rules.txt` | 해석용 의사결정나무 규칙 |
| `validation_report.md` | 종합 검증 리포트 |
| `data_caveats.md` | 데이터 가정·한계·발제사 확인 필요 항목 |
| `figures/*.png` | 시각화 |

---

## 11. 구현 시 함정 (반드시 준수)

1. **결측을 0으로 채우지 말 것.** `-99999999`는 결측이며, 결측 여부 자체가 취약주거 프록시일 수 있다. NaN 유지 + 플래그 생성.
2. **절대값 임계값 금지.** 모든 컷은 분위수 기준. 합성데이터 스케일이 실제와 다를 수 있다.
3. **직업군 코드에 의미를 부여하지 말 것.** 코드북 미확보 상태다. 원핫 인코딩만 허용.
4. **신용평점 150은 하한 절단값이다.** 연속 변수로 회귀에 그대로 투입하지 말 것. `score_floor` 플래그와 병용.
5. **T5·T6를 주 분류보다 먼저 분리할 것.** 순서가 바뀌면 T3·T4가 오염된다.
6. **연령대 15·20 구간에는 재학생이 혼입되어 있다.** 학력 컬럼이 없어 분리 불가. 리포트에서 해당 구간을 별도 표기하고, 민감도 분석으로 25세 이상만 대상으로 한 결과를 병기할 것.
7. **p값 대신 효과크기로 해석할 것.** n=100,816에서는 사소한 차이도 유의하게 나온다.
8. **합성데이터 경고.** 파생변수가 원본 변수보다 신호가 강한지 반드시 검증하고, 그렇지 않은 파생변수는 리포트에 "기여도 낮음"으로 표시할 것.
9. **개인 낙인 금지.** 유형 라벨은 개인 신용 판정이 아니라 **정책 아웃리치 우선순위**로 포지셔닝한다. 리포트 서두에 이 문구를 명시할 것.
10. **모든 난수 시드 고정.** GMM, LightGBM, train/test split 전부.

---

## 12. 실행 순서 요약

```
run.py
 ├─ 1. load()          로딩 · 센티널 처리 · 데이터 점검     → data_caveats.md
 ├─ 2. build_features() 파생변수 생성                       → features_summary.csv
 ├─ 3. build_scores()   방향 통일 · 분위수 정규화 · PCA 가중
 ├─ 4. segment()        T6 → T5 분리 → 2×2 → H flag         → segments.csv
 │     └─ gmm_crosscheck()  BIC/실루엣 · ARI · 일치율
 ├─ 5. train_reduced()  8문항 분류기 · DecisionTree 병행     → classifier.pkl
 └─ 6. validate()       프로파일 · 검정 · 시각화             → validation_report.md
```

---

## 13. 완료 기준 (Definition of Done)

- [ ] `python -m src.run --input <csv> --outdir outputs/`가 오류 없이 완주
- [ ] `outputs/` 하위 9종 산출물 전부 생성
- [ ] 6유형 전부 배정 완료, 미분류 행 0건
- [ ] T3 규모가 리포트 최상단에 출력되고, 5% 미만 시 경고 발생
- [ ] GMM 일치율 수치가 리포트에 기록
- [ ] 축소 분류기 macro F1이 리포트에 기록
- [ ] 동일 시드로 2회 실행 시 결과 동일 (재현성)
