# 부산 청년 1인가구 잠재 경제점수·6유형 추론모델 명세서

> 버전: v2.0  
> 작성 기준일: 2026-07-25  
> 구현 위치: `economic_segments/latent_model.py`  
> 학습 진입점: `python -m economic_segments.latent_run`

---

## 1. 목적

KCB 부산 청년 1인가구 합성 마이크로데이터를 이용하여 사용자별로 다음 결과를 산출한다.

1. `0~100` 범위의 상대적 경제 안정성 종합점수
2. 사용자의 금융·소득·부채 구조를 반영한 6개 경제유형
3. 해당 유형 배정에 대한 모델 확신도

외부 정답 레이블이 없으므로 임의의 점수 공식을 지도학습 타깃으로 만들지 않는다.
PCA로 잠재 경제상태를 학습하고, 동일한 잠재공간에서 GMM으로 유형을 학습하는
비지도 추론 구조를 사용한다.

이 모델의 종합점수는 미래 부도확률이나 정책 효과 예측값이 아니다. 학습 표본 안에서
사용자의 상대적인 경제 안정성을 나타내는 잠재점수다.

---

## 2. 설계 원칙

1. **가짜 정답을 만들지 않는다.** 사람이 만든 종합점수를 다시 예측하는 지도학습은 사용하지 않는다.
2. **점수와 유형은 같은 잠재공간을 사용한다.** 두 결과가 서로 다른 정보를 보지 않도록 PCA 앞 8개 차원을 공통 사용한다.
3. **방향만 도메인 지식으로 정한다.** 어떤 변수가 안정·위험 방향인지는 지정하되, 잠재성분과 가중치는 데이터에서 학습한다.
4. **학습 통계는 Train에서만 계산한다.** 결측 대체, 표준화, PCA, 점수 분위수, GMM을 Test에 적합하지 않는다.
5. **점수와 유형을 구분한다.** 점수는 연속적인 안정성, 유형은 다차원 경제 패턴을 나타낸다.
6. **불확실성을 출력한다.** 유형 코드만 반환하지 않고 GMM 사후확률의 최댓값을 확신도로 제공한다.
7. **개인 낙인 용도로 사용하지 않는다.** 정책 아웃리치와 지원 탐색을 위한 상대적 분류로만 사용한다.

---

## 3. 학습 데이터

### 3.1 데이터 개요

| 항목 | 값 |
|---|---:|
| 데이터 행 수 | 100,816명 |
| 실제 원본 컬럼 수 | 42개 |
| 결측 센티널 | `-99999999` |
| 기본 난수 시드 | 42 |
| 정답 레이블 | 없음 |

요청 문맥에서는 46개 컬럼이 언급되었으나 현재 제공된 CSV와 구현 명세의 실제 원본
스키마는 42개다. 신규 데이터에서 46개가 제공될 경우 추가 4개 컬럼은 코드북과 논리
정합성을 확인한 후 `MODEL_FEATURES` 포함 여부를 별도로 결정해야 한다.

### 3.2 데이터 분할

| 구분 | 비율 | 현재 데이터 기준 행 수 | 용도 |
|---|---:|---:|---|
| Train | 70% | 70,571 | 전처리·PCA·점수·GMM 학습 |
| Validation | 15% | 15,122 | 중간 진단 및 모델 설계 검토 |
| Test | 15% | 15,123 | 분포·확신도·논리 안정성 검증 |

분할 계층은 `연령대 × 성별`이다. 같은 시드에서는 동일한 행이 같은 split에 배정되어야 한다.

### 3.3 확정 제외 컬럼

| 컬럼 | 제외 사유 |
|---|---|
| `추정월소득` | 연소득과 중복 |
| `순자산평가금액(주택)` | 총자산과 논리적 불일치가 큼 |
| `2년내 이직후 소득 증감액` | 직장이력과 독립 생성된 정황 |
| `신용대출-총대출약정액` | 잔액과 약정액 불일치 |
| `최근 12개월 일시불이용금액` | 총 카드소비와 부분합 관계 불일치 |
| `최근 12개월 할부이용금액` | 총 카드소비와 부분합 관계 불일치 |
| `2년내 현거주지평균실거래가` | 높은 결측률 및 현재 매매가와 중복 |

조건부 제외 컬럼은 정합성 검사 위반 행의 합집합 비율이 30%를 초과할 때 자동 제외한다.

---

## 4. 모델 입력 계약

### 4.1 외부 입력

저장된 `latent_economic_model.pkl`은 원본 CSV 스키마와 동일한 42개 컬럼을 가진
`pandas.DataFrame`을 입력받는다.

```python
import joblib
import pandas as pd

model = joblib.load("outputs/latent_economic_model_final_v2/latent_economic_model.pkl")
raw_users = pd.read_csv("new_users.csv", encoding="utf-8-sig")
result = model.predict(raw_users)
```

필수 원본 컬럼이 없으면 `ValueError`를 발생시킨다. 추가 컬럼은 모델 입력에 사용하지 않는다.

### 4.2 내부 모델 피처

전처리 후 PCA가 사용하는 변수는 32개다.

#### 소득·자산

- `추정 연소득`
- `2년전 추정 연소득 금액`
- `증빙연소득`
- `총자산평가금액(주택)`
- `현 거주지의 매매가(국토부 실거래가) 또는 공시가격`
- `income_trajectory`

#### 신용·대출·상환

- `신용평점`
- `총대출건수`
- `신용대출-총대출잔액`
- `주택담보대출-총대출잔액`
- `정책자금대출-총대출잔액`
- `총 대출 상환금액 (최근 12개월)`
- `total_loan_balance`
- `avg_loan_balance`
- `dsr`

#### 소비·유동성

- `최근 12개월 신용카드소비금액`
- `최근 12개월 체크카드소비금액`
- `최근 12개월 현금서비스이용금액`
- `consumption_ratio`
- `credit_dependency`

#### 연체·위기

- `대출연체건수`
- `카드연체건수`
- `대출연체금액`
- `카드연체금액`
- `total_delinq_cnt`
- `delinq_severity`
- `Thin Filer 여부`
- `파산, 개인회생 신청 여부`

#### 고용·주거

- `2년내 직장명이력건수`
- `jeonse_income_multiple`
- `pir`
- `commute_mismatch`

---

## 5. 전처리

신규 추론과 학습에 동일한 전처리를 적용한다.

### 5.1 센티널과 수치 변환

1. 원본 42개 컬럼을 수치형으로 변환한다.
2. `-99999999`를 `NaN`으로 변환한다.
3. 확정·조건부 제외 컬럼을 제거한다.

### 5.2 전세가격 결측 대체

타깃:

```text
2년내 현거주지평균전세거래가
```

입력:

- 거주지 시군구 코드
- 연령대
- 아파트 여부
- 추정 연소득
- 현재 주거지 매매가

Train 관측분에서 Random Forest 회귀와 지역 중앙값 기준을 비교한다. Random Forest의
홀드아웃 MAE가 더 낮을 때만 모델을 채택한다.

현재 학습 결과:

| 지표 | 값 |
|---|---:|
| Random Forest MAE | 6,482.85 |
| 지역 중앙값 MAE | 6,887.76 |
| Random Forest R² | 0.163 |
| 채택 방법 | Random Forest |

### 5.3 파생변수

```text
income_trajectory = (현재 연소득 - 2년 전 연소득) / 2년 전 연소득
dsr = 최근 12개월 대출상환액 / 현재 연소득
total_loan_balance = 신용 + 주담대 + 정책자금 대출잔액
avg_loan_balance = 총대출잔액 / 대출건수
consumption_ratio = (신용카드 + 체크카드 소비액) / 연소득
credit_dependency = 신용카드 / (신용카드 + 체크카드)
total_delinq_cnt = 대출연체건수 + 카드연체건수
delinq_severity = 총연체금액 / 월소득
jeonse_income_multiple = 전세가격 / 연소득
pir = 현재 주택가격 / 연소득
commute_mismatch = 거주지 시군구와 근무지 시군구가 다름
```

분모가 0 또는 결측이면 파생값도 `NaN`으로 유지한다.

### 5.4 결측 대체와 표준화

1. Train 중앙값으로 내부 모델 피처의 결측을 대체한다.
2. 결측 지시자를 추가한다.
3. Train 평균과 표준편차로 표준화한다.

```text
z = (x - train_mean) / train_standard_deviation
```

저장된 대체값과 표준화 파라미터를 Validation, Test, 신규 사용자에게 그대로 적용한다.

---

## 6. PCA 잠재 경제상태 모델

### 6.1 목적

32개 경제변수의 중복과 상관구조를 소수의 잠재 경제요인으로 압축한다.

### 6.2 학습 설정

| 항목 | 설정 |
|---|---|
| 모델 | `sklearn.decomposition.PCA` |
| Solver | `full` |
| 성분 수 | 누적 설명분산 90% 이상 자동 선택 |
| 현재 선택 성분 | 21개 |
| 현재 누적 설명분산 | 91.40% |

### 6.3 공통 잠재공간

종합점수와 GMM 유형 분류는 PCA 앞 8개 성분을 공통으로 사용한다. 점수와 유형이
서로 다른 경제정보를 보던 이전 구조는 사용하지 않는다.

---

## 7. 종합점수

### 7.1 점수 의미

`composite_stability_score`는 높을수록 상대적으로 안정적이며 범위는 `0~100`이다.

### 7.2 안정성 앵커

앵커는 지도학습 정답이 아니라 PCA 잠재축의 부호와 경제적 방향을 결정하는 기준이다.

| 안정 방향 `(+)` | 위험 방향 `(-)` |
|---|---|
| 추정 연소득 | DSR |
| 증빙소득 | 총대출잔액 |
| 주택자산 | 대출건수 |
| 신용점수 | 소비부담 |
| 소득증가율 | 연체건수·심각도 |
|  | 현금서비스 |
|  | Thin Filer |
|  | 파산·개인회생 |
|  | 이직 횟수 |

표준화한 앵커 변수의 방향 통일 평균을 잠재 안정성 기준으로 사용한다.

### 7.3 잠재성분 선택과 가중치

PCA 앞 8개 성분 중 안정성 앵커와 절대상관이 가장 큰 최대 5개 성분을 선택한다.
가중치는 각 성분과 앵커의 상관계수를 절댓값 합이 1이 되도록 정규화한다.

현재 선택 결과:

| PCA 성분 인덱스 | 가중치 |
|---:|---:|
| 6 | -0.0911 |
| 3 | -0.1092 |
| 5 | -0.1478 |
| 4 | -0.2607 |
| 1 | -0.3912 |

```text
raw_stability = Σ(PCA_component_i × learned_weight_i)
```

### 7.4 0~100 보정

Train의 `raw_stability` 분포를 1,001개 분위수 지점으로 저장한다. 사용자 원점수를
Train 백분위로 변환하여 최종 점수를 만든다.

```text
Train 최저 안정성 ≈ 0점
Train 중앙 안정성 ≈ 50점
Train 최고 안정성 ≈ 100점
```

점수는 학습 모집단 기준 상대점수이므로 데이터 분포가 변하면 재학습 또는 재보정이 필요하다.

---

## 8. GMM 6유형 분류

### 8.1 학습 입력

PCA 앞 8개 잠재성분을 다시 표준화하여 GMM 입력으로 사용한다.

### 8.2 모델 설정

| 항목 | 설정 |
|---|---|
| 모델 | `GaussianMixture` |
| 군집 수 | 6 |
| 공분산 | `diag` |
| 초기화 반복 | 5회 |
| 정규화 항 | `1e-5` |
| 난수 시드 | CLI `--seed`, 기본 42 |

GMM은 사용자마다 6개 군집의 사후확률을 계산하고 확률이 가장 높은 군집을 선택한다.

```text
cluster = argmax P(cluster_k | latent_vector)
type_confidence = max P(cluster_k | latent_vector)
```

### 8.3 E1~E6 매핑

GMM 원시 군집번호 자체에는 의미가 없다. 군집별 평균 종합점수를 기준으로 다음과 같이
유형번호를 부여한다.

```text
평균 종합점수 최고 군집 → E1
평균 종합점수 최저 군집 → E6
```

현재 군집 매핑:

| GMM 군집 | 최종 유형 |
|---:|---|
| 4 | E1 |
| 2 | E2 |
| 0 | E3 |
| 1 | E4 |
| 3 | E5 |
| 5 | E6 |

### 8.4 최종 유형 정의

| 유형 | 쉬운 이름 | 평균점수 | 핵심 특성 |
|---|---|---:|---|
| E1 | 안정형 | 79.02 | 소득·신용이 높고 부채·상환·소비 부담이 낮음 |
| E2 | 주택대출형 | 64.22 | 소득·신용이 높고 주택담보대출 비중이 큼 |
| E3 | 저부채형 | 58.78 | 대출·연체가 적고 신용상태가 양호함 |
| E4 | 금융이력부족형 | 48.47 | 금융활동이 적고 Thin Filer 비중이 높음 |
| E5 | 대출부담형 | 19.77 | 다중대출·현금서비스·상환부담이 높음 |
| E6 | 위기형 | 11.00 | 연체·DSR·대출건수가 높고 신용점수가 낮음 |

유형명은 평균점수만으로 정하지 않고 전체 평균 대비 군집별 변수의 표준화 편차와
중앙값·발생률을 함께 검토하여 부여한다.

---

## 9. 모델 출력 계약

`predict()`는 입력 행과 동일한 인덱스를 가진 `DataFrame`을 반환한다.

| 출력 컬럼 | 타입 | 범위·예시 | 의미 |
|---|---|---|---|
| `composite_stability_score` | `float` | `0~100` | 상대적 경제 안정성 |
| `economic_type` | `string` | `E1`~`E6` | 최종 유형 코드 |
| `economic_type_name` | `string` | `안정형` 등 | 사용자 표시 이름 |
| `type_confidence` | `float` | `0~1` | 선택된 GMM 유형의 최대 사후확률 |

예시:

```json
{
  "composite_stability_score": 79.4,
  "economic_type": "E1",
  "economic_type_name": "안정형",
  "type_confidence": 0.91
}
```

### 9.1 권장 확신도 처리

| 확신도 | 운영 처리 |
|---:|---|
| `>= 0.7` | 대표 유형 단독 표시 가능 |
| `0.5~0.7` | 대표 유형과 경계 사례 안내 |
| `< 0.5` | 유형 확정 자제, 추가 확인 또는 상위 후보 제공 |

현재 공개 `predict()`는 최대확률만 반환한다. 상위 2개 유형이 필요한 서비스에서는
`gmm_.predict_proba()` 결과를 유형 매핑 순서에 맞게 변환하는 API를 추가해야 한다.

---

## 10. 검증 기준과 현재 결과

정답 레이블이 없으므로 분류 accuracy나 macro F1을 핵심 지표로 사용하지 않는다.

### 10.1 점수 방향 검증

종합점수와 주요 변수의 Spearman 상관 방향을 검사한다.

| 변수 | 기대 방향 | 현재 상관 | 결과 |
|---|---:|---:|---|
| 신용점수 | `+` | +0.659 | 통과 |
| 추정 연소득 | `+` | +0.539 | 통과 |
| 증빙소득 | `+` | +0.201 | 통과 |
| 대출건수 | `-` | -0.476 | 통과 |
| 소비부담 | `-` | -0.299 | 통과 |
| 현금서비스 | `-` | -0.270 | 통과 |
| DSR | `-` | -0.256 | 통과 |
| 연체건수 | `-` | -0.224 | 통과 |
| 연체심각도 | `-` | -0.206 | 통과 |
| 총대출잔액 | `-` | -0.157 | 통과 |
| 파산·개인회생 | `-` | -0.094 | 통과 |
| Thin Filer | `-` | -0.057 | 통과 |
| 소득증가율 | `+` | +0.029 | 통과, 관계 약함 |

현재 정의한 13개 기대 방향은 모두 통과한다.

### 10.2 유형별 점수 단조성

| 유형 | 평균 | 중앙값 |
|---|---:|---:|
| E1 | 79.02 | 84.52 |
| E2 | 64.22 | 71.47 |
| E3 | 58.78 | 59.78 |
| E4 | 48.47 | 47.61 |
| E5 | 19.77 | 15.86 |
| E6 | 11.00 | 1.76 |

- E1→E6 평균점수 단조 감소: 통과
- E1→E6 중앙값 단조 감소: 통과

### 10.3 군집 안정성

| 지표 | 현재 값 | 해석 |
|---|---:|---|
| GMM 시드 안정성 ARI | 0.9996 | 초기화 시드 변경에도 거의 동일한 군집 |
| Train/Test 유형 PSI | 0.00053 | Train과 Test 유형분포가 매우 유사 |
| Test 평균 확신도 | 0.9055 | 평균 사후확률은 높음 |
| Test 확신도 0.5 미만 | 1.57% | 일부 경계 사례 존재 |
| Train 실루엣 | 0.0595 | 군집 간 자연 분리는 약함 |

높은 GMM 확신도와 낮은 실루엣은 모순이 아니다. GMM이 학습한 확률분포 내 배정은
확실할 수 있지만, 유클리드 거리 관점에서 군집 사이 간격은 작을 수 있다.

---

## 11. 학습과 실행

### 11.1 요구 환경

```text
Python >= 3.10
pandas
numpy
scikit-learn
scipy
joblib
tabulate
```

부스팅 계열 모델은 사용하지 않는다.

### 11.2 학습 명령

```bash
cd "/home/elicer/DIVE-intergration/LLM service"

.venv/bin/python -m economic_segments.latent_run \
  --input "Dataset/(합성데이터) 종합해커톤.csv" \
  --outdir outputs/latent_economic_model_final_v2 \
  --seed 42
```

파일명의 한글 자모 정규화가 운영체제에 따라 다를 수 있으므로 실제 파일명 자동완성이나
절대경로 사용을 권장한다.

### 11.3 재학습 조건

다음 중 하나에 해당하면 재학습한다.

- 원본 컬럼 정의 또는 단위 변경
- 새로운 모집단 데이터 추가
- Train 대비 입력 분포 PSI 증가
- 점수 방향 검증 실패
- 유형별 프로파일이 기존 이름과 불일치
- 전세가격 대체모델 성능 저하
- 정책 운영상 유형 수 변경

---

## 12. 산출물

기본 출력 경로: `outputs/latent_economic_model_final_v2/`

| 파일 | 내용 |
|---|---|
| `latent_economic_model.pkl` | 원본 42개 입력부터 점수·유형까지 처리하는 저장 모델 |
| `latent_scores_and_types.csv` | 전체 사용자의 점수·유형·확신도 |
| `latent_model_metrics.json` | 학습 설정, 점수 논리검사, 안정성 지표 |
| `latent_model_report.md` | 분석 결과 요약 리포트 |
| `latent_type_profile.csv` | 유형별 주요 변수 평균·중앙값 |
| `latent_type_characteristics.csv` | 유형명과 대표 상·하위 특성 |
| `latent_type_feature_means.csv` | 유형별 변수 평균 |
| `latent_type_feature_zscores.csv` | 전체 평균 대비 유형별 표준화 편차 |
| `imputer_jeonse.pkl` | 전세가격 결측 대체모델 |
| `consistency_report.md` | 원본 논리 정합성 검사 결과 |
| `consistency_flags.csv` | 행별 정합성 위반 플래그 |
| `features_summary.csv` | 파생변수 기술통계 |

---

## 13. 구현 모듈

| 모듈 | 역할 |
|---|---|
| `economic_segments/load.py` | CSV 로딩, 스키마·센티널 처리 |
| `economic_segments/consistency.py` | 논리 정합성 검사와 컬럼 제외 |
| `economic_segments/split.py` | Train/Validation/Test 분할 |
| `economic_segments/impute.py` | 전세가격 결측 대체모델 |
| `economic_segments/features.py` | 파생변수 생성 |
| `economic_segments/latent_model.py` | PCA 점수·GMM 유형·원본 입력 추론 래퍼 |
| `economic_segments/latent_run.py` | 학습·평가·산출물 오케스트레이션 |

핵심 저장 클래스:

```text
RawLatentEconomicInference
 ├─ 전세가격 대체 artifact
 ├─ 원본 전처리 및 파생변수 생성
 └─ LatentEconomicModel
     ├─ SimpleImputer
     ├─ StandardScaler
     ├─ PCA
     ├─ 점수 분위수 보정값
     ├─ 잠재공간 StandardScaler
     └─ GaussianMixture
```

---

## 14. 알려진 한계

1. 데이터가 합성데이터이므로 실제 부산 청년 모집단의 분포와 다를 수 있다.
2. 현재 원본에는 1인가구 여부 컬럼이 없어 이미 필터링된 데이터라고 가정한다.
3. 금액 단위는 분포상 천원으로 추정하며 발제사 확인이 필요하다.
4. GMM 실루엣이 0.0595로 낮아 6개 유형의 자연 경계가 강하지 않다.
5. GMM 유형은 종합점수 하나가 아니라 8차원 잠재상태를 사용하므로 개인 점수 구간이 유형 간 겹칠 수 있다.
6. 종합점수는 상대점수라 학습 모집단이 바뀌면 동일한 원시 상태의 점수가 달라질 수 있다.
7. 유형명은 현재 데이터의 프로파일에 맞춘 이름이며 재학습 후 자동·수동 재검토가 필요하다.
8. 모델은 인과관계, 미래 부도, 정책 수혜효과를 예측하지 않는다.

---

## 15. 완료 기준

- [x] 원본 42개 컬럼 `DataFrame`을 저장 모델이 직접 입력받음
- [x] 종합점수와 유형이 동일한 PCA 잠재공간을 사용함
- [x] 종합점수 범위가 0~100임
- [x] E1 평균·중앙값이 가장 높고 E6가 가장 낮음
- [x] 13개 주요 변수의 점수 기대 방향 검증 통과
- [x] 6개 유형 모두 배정되고 미분류 행이 없음
- [x] 유형명과 군집 프로파일이 논리적으로 일치함
- [x] 유형 확신도를 함께 반환함
- [x] Train/Test 유형분포 안정성 지표를 저장함
- [x] 시드를 고정하고 모델 초기화 안정성을 검증함
- [x] 모델·점수·프로파일·검증 리포트를 파일로 저장함

